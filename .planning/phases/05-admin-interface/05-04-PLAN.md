---
phase: 05-admin-interface
plan: 04
type: execute
---

<objective>
Build dashboard with stats overview, category management, and final polish.

Purpose: Give Chamber staff a quick overview and complete the admin interface.
Output: Dashboard with key metrics, category management, polished responsive design.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-interface/05-03-SUMMARY.md

**Dashboard should show at a glance:**
- Total listings count
- Premium members count
- Categories count
- Last rotation date
- Quick actions

**Category management needs:**
- View all categories
- See listing count per category
- See premium count per category
- Note: Categories are derived from listings (no separate categories table)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build dashboard with stats cards</name>
  <files>admin/src/app/(protected)/dashboard/page.tsx, admin/src/components/dashboard/stats-card.tsx, admin/src/components/dashboard/recent-activity.tsx</files>
  <action>
1. Create stats card component (admin/src/components/dashboard/stats-card.tsx):
   - Reusable card showing metric
   - Icon, label, value, optional change indicator
   - Uses shadcn Card

2. Update dashboard page (admin/src/app/(protected)/dashboard/page.tsx):
   - Welcome message: "Welcome back, {user.email}"
   - Stats grid (4 cards):
     - Total Listings (with icon)
     - Premium Members (with icon + badge)
     - Categories (with icon)
     - Last Rotation (date display)

3. Add quick actions section:
   - "Add New Listing" button
   - "View Premium Members" button
   - "View All Listings" button

4. Add recent activity section (admin/src/components/dashboard/recent-activity.tsx):
   - Show last 5 updated listings
   - "Name was updated on Date"
   - Link to edit each listing

5. Fetch dashboard data:
   - Count total listings
   - Count premium listings
   - Count distinct categories
   - Get last rotation date (max last_rotated_at)
   - Get 5 most recently updated listings

Style notes:
- Clean 2x2 or 4-column grid for stats
- Cards with subtle shadows
- Icons from lucide-react matching sidebar

AVOID: Don't add charts/graphs (overkill for this use case). Don't add analytics.
WHY: This is a simple admin for 2-3 users, not a business intelligence dashboard.
  </action>
  <verify>
- Dashboard shows accurate counts
- Stats cards display with icons
- Quick action buttons navigate correctly
- Recent activity shows last 5 updated
  </verify>
  <done>Dashboard with stats, quick actions, and recent activity</done>
</task>

<task type="auto">
  <name>Task 2: Build categories overview page</name>
  <files>admin/src/app/(protected)/categories/page.tsx, admin/src/components/categories/category-card.tsx</files>
  <action>
1. Create categories page (admin/src/app/(protected)/categories/page.tsx):
   - Page title: "Categories"
   - Grid of category cards
   - Each card shows category stats

2. Create category card component (admin/src/components/categories/category-card.tsx):
   - Category name as title
   - Total listings count
   - Premium members count (with badge)
   - "View Listings" button (links to listings page with category filter)

3. Fetch category data:
   - GROUP BY category
   - COUNT total per category
   - COUNT premium per category
   - ORDER BY category name

4. Add category summary at top:
   - Total categories count
   - "Categories are determined by your listings. Add a listing with a new category name to create a new category."

Note: No CRUD for categories — they're derived from listing data. This is intentional to keep things simple.

Style notes:
- Cards in responsive grid
- Premium count highlighted if > 0
- Empty categories possible (if all listings deleted from category)

AVOID: Don't add category CRUD. Don't add category renaming.
WHY: Categories are just strings on listings. Renaming would require updating all listings. KISS.
  </action>
  <verify>
- Categories page shows all categories from listings
- Each card shows total and premium counts
- "View Listings" filters to that category
- Responsive grid layout
  </verify>
  <done>Categories page with overview and navigation to filtered listings</done>
</task>

<task type="auto">
  <name>Task 3: Final polish and responsive testing</name>
  <files>admin/src/app/globals.css, admin/src/components/*, various</files>
  <action>
1. Responsive design check:
   - Test all pages at mobile (375px), tablet (768px), desktop (1024px+)
   - Fix any layout issues
   - Ensure sidebar collapses to sheet on mobile
   - Ensure tables scroll horizontally on mobile
   - Ensure forms are usable on mobile

2. Loading states:
   - Add loading.tsx for each route
   - Use shadcn Skeleton for loading placeholders
   - Ensure no layout shift on load

3. Error handling:
   - Add error.tsx for each route
   - Friendly error messages
   - "Try again" button

4. Empty states:
   - All tables/lists have friendly empty states
   - Clear CTAs to add first item

5. Final visual polish:
   - Consistent spacing
   - Hover states on interactive elements
   - Focus states for accessibility
   - Smooth page transitions (if using View Transitions or similar)

6. Update sidebar active states:
   - Highlight current page in sidebar
   - Use pathname to determine active link

AVOID: Don't add fancy animations. Don't spend time on micro-interactions.
WHY: Focus on usability and reliability over flashiness.
  </action>
  <verify>
- All pages work on mobile/tablet/desktop
- Loading states show skeletons
- Error states show friendly messages
- Empty states guide user to take action
- Active navigation state shows current page
  </verify>
  <done>Admin interface polished, responsive, with proper loading/error states</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Dashboard shows accurate stats
- [ ] Quick actions work correctly
- [ ] Categories page shows all categories with counts
- [ ] All pages responsive (mobile, tablet, desktop)
- [ ] Loading states implemented
- [ ] Error states implemented
- [ ] Empty states implemented
- [ ] Navigation highlights current page
</verification>

<success_criteria>
- Complete admin interface ready for use
- Professional appearance on all devices
- Consistent UX patterns throughout
- All tasks completed
- Phase 5 complete
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-interface/05-04-SUMMARY.md`:

# Phase 5 Plan 4: Dashboard and Polish Summary

**[One-liner about what was built]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `admin/src/app/(protected)/dashboard/` - [Description]
- `admin/src/app/(protected)/categories/` - [Description]

## Decisions Made
[Key decisions during implementation]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Phase Readiness
Phase 5 complete. Admin interface fully functional.
Ready for Phase 6 (Integration & Docs).
</output>
