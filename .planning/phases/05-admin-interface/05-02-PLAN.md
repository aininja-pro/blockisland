---
phase: 05-admin-interface
plan: 02
type: execute
---

<objective>
Build the listings data table with search, sort, filter, and CRUD operations.

Purpose: Enable Chamber staff to view and manage all listings in a professional data table.
Output: Listings page with sortable/filterable table, create/edit/delete functionality.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-admin-interface/05-01-SUMMARY.md

**Key files:**
@src/models/listing.js (data model reference - fields and patterns)

**Database schema (from Phase 1):**
- id (UUID)
- goodbarber_id (string, optional)
- name (string)
- category (string)
- description (text, may contain HTML)
- address (string)
- latitude (float)
- longitude (float)
- phone (string)
- email (string)
- website (string)
- image_url (string)
- is_premium (boolean)
- rotation_position (integer)
- last_rotated_at (timestamp)
- created_at (timestamp)
- updated_at (timestamp)

**shadcn components available:** table, dialog, dropdown-menu, button, input, badge, form
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create listings data table with TanStack Table</name>
  <files>admin/src/app/(protected)/listings/page.tsx, admin/src/components/listings/data-table.tsx, admin/src/components/listings/columns.tsx</files>
  <action>
1. Install TanStack Table:
   ```
   cd admin && npm install @tanstack/react-table
   ```

2. Create columns definition (admin/src/components/listings/columns.tsx):
   - Checkbox column for bulk selection
   - Name (sortable, with link to edit)
   - Category (filterable, badge style)
   - Premium status (badge: green "Premium" or gray "Basic")
   - Address (truncated)
   - Actions dropdown (View, Edit, Delete)

3. Create DataTable component (admin/src/components/listings/data-table.tsx):
   - Use shadcn Table components
   - Column sorting (click header to sort)
   - Column filtering (category dropdown, premium status toggle)
   - Global search (filters name, address)
   - Pagination (10/25/50 per page)
   - Row selection for bulk actions

4. Create listings page (admin/src/app/(protected)/listings/page.tsx):
   - Fetch listings from Supabase on server
   - Pass to DataTable component
   - "Add Listing" button in header
   - Page title: "Listings"

5. Create Supabase query helper (admin/src/lib/queries/listings.ts):
   - getListings() - fetch all with optional filters
   - getListing(id) - fetch single listing
   - createListing(data) - insert new
   - updateListing(id, data) - update existing
   - deleteListing(id) - delete

Style notes:
- Alternating row colors (subtle)
- Hover state on rows
- Premium badge: green background
- Basic badge: gray/muted background

AVOID: Don't implement inline editing (use modal/page). Don't add export functionality yet.
WHY: Keep table focused on display and navigation to edit forms.
  </action>
  <verify>
- Listings page shows all listings in table
- Clicking column header sorts by that column
- Category filter works
- Search filters by name
- Pagination works
  </verify>
  <done>Data table displays all listings with sorting, filtering, search, pagination</done>
</task>

<task type="auto">
  <name>Task 2: Add create/edit listing dialog</name>
  <files>admin/src/components/listings/listing-form.tsx, admin/src/components/listings/listing-dialog.tsx, admin/src/app/(protected)/listings/page.tsx</files>
  <action>
1. Create listing form component (admin/src/components/listings/listing-form.tsx):
   - Use shadcn Form with react-hook-form + zod validation
   - Install: `npm install react-hook-form @hookform/resolvers zod`
   - Fields:
     - name (required, text input)
     - category (required, select/combobox with existing categories)
     - description (optional, textarea)
     - address (optional, text input)
     - phone (optional, text input)
     - email (optional, email input)
     - website (optional, url input)
     - latitude (optional, number input)
     - longitude (optional, number input)
     - image_url (optional, url input)
     - is_premium (checkbox/switch)
   - Submit button: "Create" or "Save Changes"
   - Cancel button

2. Create listing dialog (admin/src/components/listings/listing-dialog.tsx):
   - Use shadcn Dialog (Sheet for mobile)
   - Title: "Add New Listing" or "Edit Listing"
   - Contains ListingForm
   - Close on successful save
   - Show loading state during save

3. Wire up to listings page:
   - "Add Listing" button opens dialog in create mode
   - Edit action in row opens dialog in edit mode
   - Refresh table after create/update

4. Add server actions (admin/src/app/(protected)/listings/actions.ts):
   - createListingAction(formData) - validates and creates
   - updateListingAction(id, formData) - validates and updates
   - Return success/error for toast notification

5. Add toast notifications:
   ```
   npx shadcn@latest add toast sonner
   ```
   - Show success toast on save
   - Show error toast on failure

AVOID: Don't add image upload yet (just URL input). Don't add map picker for coordinates.
WHY: Keep scope manageable. Image upload and map picker can be future enhancements.
  </action>
  <verify>
- Click "Add Listing" → dialog opens with empty form
- Fill form and submit → listing appears in table, toast shows success
- Click Edit on row → dialog opens with pre-filled data
- Update and save → changes reflected in table
  </verify>
  <done>Create and edit listings via modal dialog, with validation and toast feedback</done>
</task>

<task type="auto">
  <name>Task 3: Add delete functionality with confirmation</name>
  <files>admin/src/components/listings/delete-dialog.tsx, admin/src/app/(protected)/listings/actions.ts</files>
  <action>
1. Create delete confirmation dialog (admin/src/components/listings/delete-dialog.tsx):
   - Use shadcn AlertDialog
   - Title: "Delete Listing"
   - Description: "Are you sure you want to delete {listing.name}? This action cannot be undone."
   - Cancel button (closes dialog)
   - Delete button (red/destructive variant)
   - Loading state during deletion

2. Add delete server action:
   - deleteListingAction(id) in actions.ts
   - Return success/error

3. Wire up delete:
   - Delete action in row dropdown triggers confirmation dialog
   - On confirm: delete and refresh table
   - Show success/error toast

4. Add bulk delete (optional but nice):
   - When rows selected, show "Delete Selected" button
   - Confirmation shows count: "Delete 3 listings?"
   - Bulk delete action

AVOID: Don't add soft delete. Don't add undo functionality.
WHY: Hard delete is simpler and acceptable for this use case. Data isn't precious.
  </action>
  <verify>
- Click Delete on row → confirmation dialog appears
- Cancel → dialog closes, no deletion
- Confirm → listing deleted, removed from table, toast shows success
- Bulk selection + delete works
  </verify>
  <done>Delete listings with confirmation, single and bulk delete working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Listings page displays all listings in data table
- [ ] Sorting works on name, category columns
- [ ] Filtering by category works
- [ ] Search filters listings
- [ ] Pagination works (10/25/50 per page)
- [ ] Create new listing via dialog
- [ ] Edit existing listing via dialog
- [ ] Delete listing with confirmation
- [ ] Toast notifications for all actions
</verification>

<success_criteria>
- Full CRUD for listings implemented
- Data table with professional UX (sort, filter, search, paginate)
- Form validation with helpful error messages
- Confirmation dialog for destructive actions
- All tasks completed
</success_criteria>

<output>
After completion, create `.planning/phases/05-admin-interface/05-02-SUMMARY.md`:

# Phase 5 Plan 2: Listings Table Summary

**[One-liner about what was built]**

## Accomplishments
- [Key outcomes]

## Files Created/Modified
- `admin/src/components/listings/` - [Description]

## Decisions Made
[Key decisions during implementation]

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Ready for 05-03-PLAN.md (Premium management)
</output>
